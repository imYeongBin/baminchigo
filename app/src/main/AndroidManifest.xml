<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.techtown.baminchigo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".regMember"></activity>
        <activity
            android:name=".mngEtc"
            android:label="@string/title_activity_mng_etc"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".mngAttend"
            android:label="@string/title_activity_mng_attend"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".mngMember" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>